{
  "name": "canpl-analytics",
  "version": "2.0.0",
  "description": "The most comprehensive open dataset for the Canadian Premier League (CPL).",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "example": "examples"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "npm test && node -e \"JSON.parse(require('fs').readFileSync('vercel.json')); JSON.parse(require('fs').readFileSync('public/openapi.json'))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^24.35.0"
  }
}
